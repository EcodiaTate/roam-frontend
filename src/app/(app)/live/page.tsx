// src/app/(app)/live/page.tsx
// Online-only live trip — no auth required (route already fetched).
import LiveTripClientPage from "./ClientPage";

export default function Page() {
  return <LiveTripClientPage />;
}
