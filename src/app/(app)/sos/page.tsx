// Rendered by PersistentTabs in (app)/layout.tsx - this route page is a no-op.
// Kept so Next.js registers the /sos route for URL resolution + static export.
export default function EmergencyPage() {
  return null;
}
