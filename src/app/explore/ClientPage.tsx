// src/app/(shell)/explore/ClientPage.tsx
"use client";

export function ExploreClientPage() {
  return (
    <div className="roam-page">
      <h1 className="roam-h1">Explore</h1>
      <p className="roam-muted">Search places, browse POIs, and explore the map without activating a trip.</p>

      <div className="roam-card">
        <p className="roam-muted">Next: search UI + results sheet + optional map preview.</p>
      </div>
    </div>
  );
}
